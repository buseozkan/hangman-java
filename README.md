# Hangman game without hanging 
`git clone` the repository

# To play the game
`java files/main.java` 

start guessing the characters

# You have maximum number of try chances!


* Make sure that the Java is installed
